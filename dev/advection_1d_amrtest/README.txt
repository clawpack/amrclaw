
Debugging 1d amr using 2d amrclaw by setting my=1 and 
  amrdata.refinement_ratios_y = [1,1].

Example taken from advection_2d_square with minimal changes, needs cleaning up as
example.

Currently seems to work with no limiter, but bombs at random times with limiters.

